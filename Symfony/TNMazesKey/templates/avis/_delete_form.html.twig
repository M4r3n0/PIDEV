<form method="post" action="{{ path('app_avis_delete', {'numavis': avi.numavis}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ avi.numavis) }}">
    <button class="btn">Delete</button>
</form>
